streamlit
pandas
requests
matplotlib
numpy
python-dotenv
yfinance

# ---- Core ----
pandas>=2.2          # 新版内置 Arrow 支持
pyarrow>=15.0.0      # Parquet/Arrow backend

# ---- Async HTTP ----
aiohttp>=3.9

# ── Rate-limiting ────────────────────────────────────────
aiolimiter>=1.0.0        # throttle concurrent requests

# ── Retry logic ─────────────────────────────────────────
tenacity>=8.0.1          # retry decorator for call_api

# ---- Utilities ----
python-dotenv>=1.0   # 读取 .env
rich>=13.7           # 彩色进度条

# ---- 可选：版本锁定更严格 ----
# aiohttp==3.9.5
# pandas==2.2.2