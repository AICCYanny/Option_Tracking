# Option_Tracking

This is a Streamlit app for tracking and analyzing options data using the iVolatility API.  
It includes `.streamlit/`, `app.py`, and `requirements.txt` for deployment.

## Features

- Interactive option data charts
- Abnormal flow detection
- Raw IV & Greeks visualization

## How to Run

```bash
streamlit run app.py